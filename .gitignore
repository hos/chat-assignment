# yarn berry
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

node_modules

# This directory will be used by PGLite to store the data of PostgreSQL database.
data


*.env

# This is pre-configured env vars for running with docker-compose,
# if you want to override them, create a development.env file in the root of the project.
!.env

# This is pre-configured env vars for running tests, so we will need to commit this one too.
!test.env
